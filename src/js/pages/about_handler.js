// const { loadConfig } = require("../handler/files");
const { ipcRenderer } = require("electron");

// let config = loadConfig();

// if (!config.success) {
// 	ipcRenderer.send("dialogbox", ["error", config.errMsg]);
// }
